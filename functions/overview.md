# Backend Directory

This directory contains the core logic and configuration for interacting with Firebase services.

## Files
- `firebase.js`: Initializes the Firebase app and exports service instances (Auth, Firestore, Storage).
- `storage.js`: Encapsulates logic for interacting with Firebase Cloud Storage.